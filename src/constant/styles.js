const recovery = Object.freeze({
  display: 'flex',
  alignItems: 'center',
  width: '100%',
  flexDirection: 'column',
  height: '100%',
  justifyContent: 'center',
});

export default { recovery };
