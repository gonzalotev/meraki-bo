export const selectDuties = (state) => state.duty.duties;

export const selectEnrollment = (state) => state.duty.enrollment;

export const selectDuty = (state) => state.duty.duty;

export const selectStatus = (state) => state.duty.status;
