export const selectInscriptions = (state) => state.inscription.inscriptions;

export const selectInscription = (state) => state.inscription.inscription;

export const selectStatus = (state) => state.inscription.status;
